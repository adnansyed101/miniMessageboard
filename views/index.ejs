<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="/new">Form</a>

    <% messages.map((message) => { %>
    <h2><%= message.user%></h2>
    <p><%= message.text%></p>
    <strong><%= message.added%></strong>
    <% }) %>
  </body>
</html>
